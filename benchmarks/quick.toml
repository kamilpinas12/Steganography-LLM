# Szybki smoke test — tylko demo generacji, bez lm-eval.

[run]
tests = ["demo"]

[model]
key = "tinyllama"

[stego]
top_n = 15
threshold = 0.01
seed = 42

[demo]
prompt = "My favorite programming language is"
max_new_tokens = 32

[output]
results_dir = "results"
