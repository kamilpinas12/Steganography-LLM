# Tylko lm-evaluation-harness — sweep po progach threshold.

[run]
tests = ["harness"]

[model]
key = "tinyllama"

[stego]
top_n = 15
thresholds = [0.001, 0.01, 0.05, 0.1]
seed = 42

[harness]
tasks = ["wikitext"]
batch_size = 1
limit = 20

[output]
results_dir = "results"
